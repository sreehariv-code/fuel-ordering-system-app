import { ScrollView, Text, Image } from "react-native";

const Profile = () => {
  return (
    <ScrollView>
      <Text>This is Profile Page</Text>
    </ScrollView>
  );
};

export default Profile;
